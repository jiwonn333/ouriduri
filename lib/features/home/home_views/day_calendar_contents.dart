import 'package:flutter/cupertino.dart';

class DayCalendarContents extends StatelessWidget {
  const DayCalendarContents({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
